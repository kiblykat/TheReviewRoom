# Test logging configuration. 
# logging.config=classpath:logback-controllers.xml

# Add a Spring profile to our test by using the ActiveProfiles annotation, for example @ActiveProfiles("logging-test"). 
# logging.level.sg.com.smartinventory.controllers=TRACE
# logging.level.root=ERROR

# Profile management. 
# Set the default active profile. 
# Use the command to activate: mvn test -Dspring.profiles.active=development. 
spring.profiles.active=development
# Set the default default profile. This is the profile that is used on any bean if you don't specify the @Profile annotation to be a certain profile. 
# spring.profiles.default=development